import { noStoreHeaders } from "../auth/middleware.ts";

const styles = `
:root{color-scheme:light;--ink:#17231d;--muted:#66726b;--paper:#f4f3ec;--card:#fffef9;--line:#d9ddd3;--green:#b8f36a;--deep:#1b3026;--red:#9c3e35;font-family:ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}
*{box-sizing:border-box}body{margin:0;min-height:100vh;background:radial-gradient(ellipse at 12% 8%,#e3eccd 0,transparent 35%),var(--paper);color:var(--ink)}a{color:inherit}button,input,select{font:inherit}button{cursor:pointer}.shell{max-width:1120px;margin:auto;padding:28px 28px 72px}.top{display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line);padding:0 0 18px}.brand{display:flex;align-items:center;gap:11px;font-weight:750;letter-spacing:-.03em}.mark{width:30px;height:30px;display:grid;place-items:center;background:var(--deep);color:var(--green);border-radius:9px;font:700 14px Georgia,serif}.top small,.eyebrow,.label{font:600 11px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.12em;text-transform:uppercase;color:var(--muted)}.layout{display:grid;grid-template-columns:minmax(280px,.82fr) minmax(400px,1.18fr);gap:clamp(36px,7vw,100px);align-items:center;padding:clamp(48px,9vh,104px) 0 0}.intro{max-width:430px}.eyebrow{color:#557740}.intro h1{font:500 clamp(42px,6vw,72px)/.99 Georgia,"Times New Roman",serif;letter-spacing:-.055em;margin:20px 0}.intro p{font-size:16px;line-height:1.7;color:var(--muted);max-width:390px}.accent{display:inline-block;margin-top:34px;padding:10px 13px;border:1px solid var(--line);border-radius:999px;color:#49574d;font:12px ui-monospace,SFMono-Regular,Menlo,monospace}.card{background:var(--card);border:1px solid #e0e2da;border-radius:18px;padding:clamp(24px,4vw,40px);box-shadow:0 22px 70px #2533230d}.card h2{font:500 30px/1.1 Georgia,"Times New Roman",serif;margin:0 0 8px;letter-spacing:-.035em}.sub{margin:0 0 26px;color:var(--muted);font-size:14px;line-height:1.5}.field{display:grid;gap:7px;margin:14px 0}.field label{font-size:12px;font-weight:650}.field input,.field select{width:100%;height:46px;border:1px solid var(--line);border-radius:9px;padding:0 13px;background:#fff;color:var(--ink);outline:none}.field input:focus,.field select:focus{border-color:#799e4f;box-shadow:0 0 0 3px #b8f36a45}.primary{width:100%;border:0;border-radius:9px;background:var(--deep);color:white;padding:14px 16px;font-weight:700;margin-top:8px}.primary:hover{background:#274535}.secondary{border:1px solid var(--line);border-radius:8px;background:white;color:var(--ink);padding:10px 13px;font-weight:650}.links{display:flex;justify-content:space-between;gap:14px;margin:20px 0 0;font-size:13px;color:var(--muted)}.links button{padding:0;border:0;background:none;color:#385d3b;text-decoration:underline;text-underline-offset:3px}.divider{display:flex;align-items:center;gap:14px;color:#8b948c;font:11px ui-monospace,monospace;margin:20px 0}.divider:before,.divider:after{content:"";height:1px;background:var(--line);flex:1}.notice{margin-top:16px;padding:11px 13px;background:#edf4e6;border-radius:8px;font-size:13px;color:#385136}.error{color:var(--red);font-size:13px;min-height:18px;margin:10px 0}.hidden{display:none!important}.token-head{display:flex;justify-content:space-between;align-items:flex-start;gap:24px}.token-list{display:grid;gap:10px;margin-top:24px}.token-row{display:grid;grid-template-columns:1fr auto;align-items:center;gap:14px;border:1px solid var(--line);border-radius:10px;padding:14px}.token-row strong{display:block;font-size:14px}.token-row small{display:block;margin-top:5px;color:var(--muted);font:11px ui-monospace,monospace}.token-secret{padding:14px;border:1px dashed #8eae6b;background:#f3f8eb;border-radius:10px;overflow-wrap:anywhere;font:12px ui-monospace,monospace}.user-code{font:700 clamp(28px,5vw,42px)/1 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.16em;background:#f0f3e9;border-radius:12px;padding:20px;text-align:center}.steps{display:grid;gap:12px;padding:0;list-style:none;counter-reset:step}.steps li{counter-increment:step;display:flex;gap:12px;color:var(--muted);font-size:13px;line-height:1.5}.steps li:before{content:counter(step,decimal-leading-zero);color:#6a8b4e;font:12px ui-monospace,monospace}.footer{margin-top:24px;color:var(--muted);font-size:12px;line-height:1.6}.mono{font:12px ui-monospace,SFMono-Regular,Menlo,monospace}@media(max-width:800px){.shell{padding:20px 18px 48px}.layout{grid-template-columns:1fr;gap:30px;padding-top:44px}.intro h1{font-size:50px}.intro p{font-size:14px}.card{padding:24px}}
`;

const pageSecurityHeaders = {
  "Content-Type": "text/html; charset=utf-8",
  "Content-Security-Policy": "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self'; object-src 'none'; base-uri 'none'; form-action 'self'; frame-ancestors 'none'",
  "X-Frame-Options": "DENY",
  "X-Content-Type-Options": "nosniff",
  "Referrer-Policy": "no-referrer",
};

export function authLoginPage(returnTo: string): Response {
  const safeReturnTo = safeLocalPath(returnTo) ?? "/settings/api-tokens";
  const body = `
  <div class="layout"><section class="intro"><div class="eyebrow">Private artifact workbench</div><h1>Ship work.<br>Keep it yours.</h1><p>Sign in to manage team-scoped publishing devices and browse artifacts through the gateway. Your files stay in private object storage.</p><div class="accent">IDENTITY · TEAMS · ARTIFACTS</div></section>
  <section class="card"><h2 id="form-title">Welcome back</h2><p class="sub" id="form-sub">Sign in to your Artifact Sync workspace.</p>
    <form id="email-form"><div id="name-wrap" class="field hidden"><label for="name">Your name</label><input id="name" name="name" autocomplete="name" maxlength="100"></div><div class="field"><label for="email">Email address</label><input id="email" name="email" type="email" autocomplete="email" required></div><div class="field"><label for="password">Password</label><input id="password" name="password" type="password" autocomplete="current-password" minlength="8" required></div><p id="form-error" class="error" aria-live="polite"></p><button class="primary" type="submit" id="submit-button">Sign in</button></form>
    <div class="divider">OR CONTINUE WITH</div><button class="secondary" id="github-button" type="button">Continue with GitHub</button><div class="links"><span id="mode-hint">New here?</span><button id="mode-button" type="button">Create account</button></div><div class="links"><button id="forgot-button" type="button">Forgot password?</button></div><p class="footer">New accounts verify their email before publishing access is enabled. A personal team is created automatically after verification.</p>
  </section></div>`;
  const script = `
  const returnTo=${inlineScriptJson(safeReturnTo)};let mode='signin';const form=document.querySelector('#email-form');const error=document.querySelector('#form-error');
  const setMode=(next)=>{mode=next;document.querySelector('#form-title').textContent=next==='signup'?'Create your account':'Welcome back';document.querySelector('#form-sub').textContent=next==='signup'?'Your private artifact workspace starts with a team of one.':'Sign in to your Artifact Sync workspace.';document.querySelector('#name-wrap').classList.toggle('hidden',next!=='signup');document.querySelector('#submit-button').textContent=next==='signup'?'Create account':'Sign in';document.querySelector('#mode-hint').textContent=next==='signup'?'Already have an account?':'New here?';document.querySelector('#mode-button').textContent=next==='signup'?'Sign in':'Create account';document.querySelector('#forgot-button').classList.toggle('hidden',next==='signup');document.querySelector('#password').autocomplete=next==='signup'?'new-password':'current-password';error.textContent='';};
  document.querySelector('#mode-button').addEventListener('click',()=>setMode(mode==='signin'?'signup':'signin'));
  form.addEventListener('submit',async(event)=>{event.preventDefault();error.textContent='';const payload={email:document.querySelector('#email').value,password:document.querySelector('#password').value,callbackURL:returnTo};if(mode==='signup')payload.name=document.querySelector('#name').value;try{const response=await fetch('/__api/auth/'+(mode==='signup'?'sign-up/email':'sign-in/email'),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const data=await response.json();if(!response.ok)throw new Error(data.message||'Sign-in could not be completed.');if(mode==='signup'&&!data.token){document.querySelector('#form-sub').textContent='Check your inbox for the verification link, then return here to sign in.';form.reset();return;}location.assign(returnTo);}catch(e){error.textContent=e instanceof Error?e.message:'Sign-in could not be completed.';}});
  document.querySelector('#github-button').addEventListener('click',async()=>{error.textContent='';try{const response=await fetch('/__api/auth/sign-in/social',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({provider:'github',callbackURL:returnTo})});const data=await response.json();if(!response.ok||!data.url)throw new Error(data.message||'GitHub sign-in is unavailable.');location.assign(data.url);}catch(e){error.textContent=e instanceof Error?e.message:'GitHub sign-in is unavailable.';}});
  document.querySelector('#forgot-button').addEventListener('click',async()=>{const email=document.querySelector('#email').value;if(!email){error.textContent='Enter your email address first.';return;}error.textContent='';try{const response=await fetch('/__api/auth/request-password-reset',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,redirectTo:returnTo})});if(!response.ok)throw new Error('Password reset could not be requested.');document.querySelector('#form-sub').textContent='If the account exists, a password-reset link is on its way.';}catch(e){error.textContent=e instanceof Error?e.message:'Password reset could not be requested.';}});`;
  return htmlPage("Sign in · Artifact Sync", body, script);
}

export function apiTokensPage(): Response {
  const body = `
  <div class="layout"><section class="intro"><div class="eyebrow">Device access</div><h1>One key.<br>One team.</h1><p>Create a separate credential for each machine. Each credential can publish to exactly one team and can be revoked without disturbing your account login.</p><div class="accent">ROTATING · TEAM-BOUND · REVOCABLE</div></section>
  <section class="card"><div class="token-head"><div><h2>API tokens</h2><p class="sub">The secret is shown once. Store it in your local credential file, never in a command argument.</p></div><a class="secondary" href="/auth/device">Connect a device</a></div>
    <form id="create-form"><div class="field"><label for="token-name">Device name</label><input id="token-name" maxlength="64" placeholder="Ashutosh’s workstation" required></div><div class="field"><label for="team-select">Authorized team</label><select id="team-select" required></select></div><p id="form-error" class="error" aria-live="polite"></p><button class="primary" type="submit">Create team-scoped token</button></form>
    <div id="new-token" class="hidden"><div class="notice">Copy this secret now. It will not be displayed again.</div><p id="token-value" class="token-secret"></p><button class="secondary" id="copy-token" type="button">Copy token</button><p class="footer">The CLI exchanges this refresh credential for a seven-day access token and rotates it when renewed.</p></div>
    <div id="token-list" class="token-list"></div><p class="footer">Refresh credentials expire after 30 days without use and no later than 90 days after creation. Revoking one stops future refreshes immediately; access JWTs already issued may remain valid for up to seven days.</p>
  </section></div>`;
  const script = `
  const error=document.querySelector('#form-error');const teams=document.querySelector('#team-select');const list=document.querySelector('#token-list');const json=async(url,options={})=>{const response=await fetch(url,{...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});if(response.status===401){location.assign('/auth/login?returnTo='+encodeURIComponent('/settings/api-tokens'));throw new Error('Please sign in again.');}const data=response.status===204?{}:await response.json();if(!response.ok)throw new Error(data.message||data.error||'Request could not be completed.');return data;};
  const load=async()=>{try{const [teamData,tokenData]=await Promise.all([json('/__api/v1/teams'),json('/__api/v1/api-tokens')]);teams.replaceChildren(...teamData.teams.map(team=>{const option=document.createElement('option');option.value=team.id;option.textContent=team.name+' · '+team.slug;return option;}));list.replaceChildren(...tokenData.tokens.map(token=>{const row=document.createElement('div');row.className='token-row';const detail=document.createElement('div');const name=document.createElement('strong');name.textContent=token.name;const meta=document.createElement('small');meta.textContent=token.team+' · '+(token.revokedAt?'revoked':'expires '+new Date(token.expiresAt).toLocaleDateString());detail.append(name,meta);row.append(detail);if(!token.revokedAt){const button=document.createElement('button');button.className='secondary';button.textContent='Revoke';button.addEventListener('click',async()=>{try{await json('/__api/v1/api-tokens/'+encodeURIComponent(token.id),{method:'DELETE'});await load();}catch(e){error.textContent=e.message;}});row.append(button);}return row;}));if(!tokenData.tokens.length){const empty=document.createElement('p');empty.className='footer';empty.textContent='No API tokens yet. Create one for your CLI or another trusted device.';list.append(empty);}}catch(e){error.textContent=e.message;}};
  document.querySelector('#create-form').addEventListener('submit',async(event)=>{event.preventDefault();error.textContent='';try{const data=await json('/__api/v1/api-tokens',{method:'POST',body:JSON.stringify({name:document.querySelector('#token-name').value,teamId:teams.value})});document.querySelector('#token-value').textContent=data.token;document.querySelector('#new-token').classList.remove('hidden');document.querySelector('#token-name').value='';await load();}catch(e){error.textContent=e.message;}});
  document.querySelector('#copy-token').addEventListener('click',async()=>{await navigator.clipboard.writeText(document.querySelector('#token-value').textContent);document.querySelector('#copy-token').textContent='Copied';});load();`;
  return htmlPage("API tokens · Artifact Sync", body, script, true);
}

export function devicePage(): Response {
  const body = `
  <div class="layout"><section class="intro"><div class="eyebrow">Secure device authorization</div><h1>Pair a<br>workstation.</h1><p>Approve the code shown by <span class="mono">artifact-sync login</span>. This authorizes that device for one team only.</p><ol class="steps"><li>Keep the CLI waiting while this page is open.</li><li>Choose the team this workstation may publish to.</li><li>Approve only if you initiated the request.</li></ol><div class="accent">CODES EXPIRE AFTER 10 MINUTES</div></section>
  <section class="card"><h2>Authorize device</h2><p class="sub">Confirm the short code printed by your CLI.</p><form id="device-form"><div class="field"><label for="user-code">Device code</label><input id="user-code" class="user-code" autocomplete="one-time-code" maxlength="8" pattern="[A-HJ-NP-Z2-9]{8}" required></div><div class="field"><label for="team-select">Grant access to</label><select id="team-select" required></select></div><p id="form-error" class="error" aria-live="polite"></p><button class="primary" type="submit">Approve this device</button></form><div id="done" class="notice hidden">Device approved. Return to your terminal; it will finish connecting automatically.</div><p class="footer">The CLI receives a refresh credential after approval. It is stored locally with restrictive file permissions and is never sent to object storage.</p></section></div>`;
  const script = `
  const params=new URLSearchParams(location.search);const original=(params.get('code')||'').toUpperCase();const codeInput=document.querySelector('#user-code');const form=document.querySelector('#device-form');const error=document.querySelector('#form-error');const select=document.querySelector('#team-select');codeInput.value=original;
  const load=async()=>{const response=await fetch('/__api/v1/teams');if(response.status===401){const returnTo=location.pathname+location.search;location.assign('/auth/login?returnTo='+encodeURIComponent(returnTo));return;}const data=await response.json();select.replaceChildren(...data.teams.map(team=>{const option=document.createElement('option');option.value=team.id;option.textContent=team.name+' · '+team.slug;return option;}));};
  form.addEventListener('submit',async(event)=>{event.preventDefault();error.textContent='';const userCode=codeInput.value.trim().toUpperCase();try{const response=await fetch('/__api/v1/device/approve',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({userCode,teamId:select.value})});if(!response.ok){const data=await response.json();throw new Error(data.error||'Approval failed.');}form.classList.add('hidden');document.querySelector('#done').classList.remove('hidden');}catch(e){error.textContent=e instanceof Error?e.message:'Approval failed.';}});load();`;
  return htmlPage("Authorize a device · Artifact Sync", body, script, true);
}

function htmlPage(title: string, body: string, script: string, showSettingsLink = false): Response {
  const settingsLink = showSettingsLink ? `<a href="/settings/api-tokens" class="mono">TOKEN SETTINGS</a>` : `<span class="mono">ARTIFACT SYNC</span>`;
  const document = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="light"><title>${escapeHtml(title)}</title><style>${styles}</style></head><body><main class="shell"><header class="top"><div class="brand"><span class="mark">A</span>Artifact Sync</div>${settingsLink}</header>${body}</main><script>${script}</script></body></html>`;
  return new Response(document, { headers: noStoreHeaders(pageSecurityHeaders) });
}

function safeLocalPath(path: string): string | null {
  if (!path.startsWith("/") || path.startsWith("//") || path.includes("\\") || path.includes("\0")) return null;
  return path;
}

function inlineScriptJson(value: string): string {
  return JSON.stringify(value)
    .replaceAll("<", "\\u003c")
    .replaceAll(">", "\\u003e")
    .replaceAll("&", "\\u0026")
    .replaceAll("\u2028", "\\u2028")
    .replaceAll("\u2029", "\\u2029");
}

function escapeHtml(value: string): string {
  return value.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#39;");
}
